export class Shape {
    _x
    _y

    constructor(x, y) {
        this._x = x
        this._y = y
        this._lineWidth = this._lineWidth
    }

set setLineWith(lineWidth) {
    this._lineWidth = lineWidth
}
}